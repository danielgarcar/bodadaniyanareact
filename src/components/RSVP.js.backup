import React, { useState } from 'react';
import { supabase } from '../supabaseClient';
import './RSVP.css';

const RSVP = () => {
  const [formData, setFormData] = useState({
    name: '',
    email: '',
    phone: '',
    attendance: '',
    guests: 1,
    allergies: '',
    song: '',
    message: ''
  });
  const [isSubmitted, setIsSubmitted] = useState(false);

  const handleChange = (e) => {
    setFormData({
      ...formData,
      [e.target.name]: e.target.value
    });
  };

  const handleSubmit = async (e) => {
    e.preventDefault();
    // Guardar RSVP en Supabase
    const { data, error } = await supabase.from('rsvp').insert([
      {
        status: formData.attendance === 'yes' ? 'confirmado' : 'no',
        guests_count: formData.guests,
        message: formData.message,
        // Puedes agregar más campos si la tabla los tiene
      }
    ]);
    setIsSubmitted(true);
  };

  if (isSubmitted) {
    return (
      <div className="rsvp-container">
        <section className="rsvp-success">
          <div className="container">
            <div className="success-content">
              <div className="success-icon">
                <i className="fas fa-heart"></i>
              </div>
              <h1>¡Confirmación recibida!</h1>
              <p>Gracias por confirmar tu asistencia. ¡Esperamos celebrar contigo este día tan especial!</p>
              <div className="success-actions">
                <a href="/accommodation" className="success-btn primary">
                  <i className="fas fa-bed"></i>
                  Reservar Hotel
                </a>
                <a href="/faq" className="success-btn secondary">
                  <i className="fas fa-info-circle"></i>
                  Ver Información
                </a>
              </div>
            </div>
          </div>
        </section>
      </div>
    );
  }

  return (
    <div className="rsvp-container">
      {/* Hero Section */}
      <section className="rsvp-hero">
        <div className="container">
          <div className="hero-content">
            <h1 className="rsvp-title">Confirma tu Asistencia</h1>
            <p className="rsvp-subtitle">
              Tu presencia hará que este día sea perfecto
            </p>
            <div className="rsvp-deadline">
              <i className="fas fa-calendar-alt"></i>
              Fecha límite: 1 de Julio, 2026
            </div>
          </div>
        </div>
      </section>

      {/* Form Section */}
      <section className="rsvp-form-section">
        <div className="container">
          <div className="form-wrapper">
            <form onSubmit={handleSubmit} className="rsvp-form">
              <div className="form-step">
                <h3>Información Personal</h3>
                <div className="form-row">
                  <div className="form-group">
                    <label htmlFor="name">Nombre completo</label>
                    <input
                  type="text"
                  id="name"
                  name="name"
                  value={formData.name}
                  onChange={handleChange}
                  required
                />
              </div>
              <div className="form-group">
                <label htmlFor="email">Email *</label>
                <input
                  type="email"
                  id="email"
                  name="email"
                  value={formData.email}
                  onChange={handleChange}
                  required
                />
              </div>
            </div>

            <div className="form-row">
              <div className="form-group">
                <label htmlFor="phone">Teléfono</label>
                <input
                  type="tel"
                  id="phone"
                  name="phone"
                  value={formData.phone}
                  onChange={handleChange}
                />
              </div>
              <div className="form-group">
                <label htmlFor="guests">Número de acompañantes</label>
                <select
                  id="guests"
                  name="guests"
                  value={formData.guests}
                  onChange={handleChange}
                >
                  <option value={0}>Solo yo</option>
                  <option value={1}>1 acompañante</option>
                  <option value={2}>2 acompañantes</option>
                  <option value={3}>3 acompañantes</option>
                </select>
              </div>
            </div>

            <div className="form-group">
              <label>¿Podrás acompañarnos? *</label>
              <div className="radio-group">
                <label className="radio-label">
                  <input
                    type="radio"
                    name="attendance"
                    value="yes"
                    checked={formData.attendance === 'yes'}
                    onChange={handleChange}
                    required
                  />
                  <span>Sí, allí estaré 🎉</span>
                </label>
                <label className="radio-label">
                  <input
                    type="radio"
                    name="attendance"
                    value="no"
                    checked={formData.attendance === 'no'}
                    onChange={handleChange}
                    required
                  />
                  <span>No podré asistir 😢</span>
                </label>
              </div>
            </div>

            <div className="form-group">
              <label htmlFor="allergies">Alergias o restricciones alimentarias</label>
              <textarea
                id="allergies"
                name="allergies"
                value={formData.allergies}
                onChange={handleChange}
                placeholder="Por favor, indica cualquier alergia o restricción dietética..."
              />
            </div>

            <div className="form-group">
              <label htmlFor="song">Canción que no puede faltar</label>
              <input
                type="text"
                id="song"
                name="song"
                value={formData.song}
                onChange={handleChange}
                placeholder="¿Qué canción te haría bailar toda la noche?"
              />
            </div>

              <div className="form-group">
                <label htmlFor="message">Mensaje para los novios</label>
                <textarea
                  id="message"
                  name="message"
                  value={formData.message}
                  onChange={handleChange}
                  placeholder="Dinos algo bonito... ¡nos encanta leeros! 💕"
                />
              </div>
            </div>
  
              <button type="submit" className="btn btn-primary submit-btn">
              <i className="fas fa-heart"></i>
              Confirmar Asistencia
            </button>
          </form>
        </div>
      </section>
    </div>
  );
};

export default RSVP;
